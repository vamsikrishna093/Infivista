version: '3.8'

services:
  infivista:
    build: .
    image: infivista:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/", "||", "exit", "1"]
      interval: 30s
      timeout: 5s
      retries: 3
